import random

# Random Number Generator in Python
lower_bound = input("Enter lower bound for random number: ")
upper_bound = input("Enter upper bound for random number: ")

# HMM will try to predict the correct function 
# to generate based off the random module import
# along with the lower and upper bound